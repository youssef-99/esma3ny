import 'package:flutter/material.dart';

class TherapistProfileState extends ChangeNotifier {
  // get all profile data
  //listen on it from every topic
  // edit and get all data to change current state
  // handle errors
}
